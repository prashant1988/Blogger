<p>Tags: <%= tag_links(@article.tags)%></p>
<h1>Article Tagged with<%= @tag.name%></h1>
<ul>
<% @tag.articles.each do |article| %>
<li>
<%= link_to article.title, article_path(article)%>

<% end %>
</li>
</ul>